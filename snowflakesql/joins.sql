SELECT  a.COUNTRY_REGION,  SUM(b.CASES) as total_cases,  SUM(b.DEATHS) as total_deaths,AVG(a.GROCERY_AND_PHARMACY_CHANGE_PERC) FROM GOOG_GLOBAL_MOBILITY_REPORT a INNER JOIN ECDC_GLOBAL b ON a.COUNTRY_REGION = b.COUNTRY_REGION GROUP BY a.country_region